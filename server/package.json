{
  "name": "rps-stratego-server",
  "version": "1.0.0",
  "description": "RPS Stratego - Backend with Socket.io",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "npx prisma generate && node index.js",
    "dev": "node --watch index.js",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:generate": "prisma generate",
    "prisma:studio": "prisma studio",
    "prisma:seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "prisma": "^5.10.0",
    "socket.io": "^4.7.2"
  }
}
